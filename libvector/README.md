# Algoritmo de Quicksort en C++

## Compilación

~~~
$ g++ -o sort-vector.o -Wall -O1 -std=c++11 headers/vectorutils.cpp src/sort-vector.cpp
~~~

## Ejecución

(-p imprime los vectores)

~~~
$ ./sort-vector.o -n tamaño [-p]
~~~
